name: 💿 CI Staging
on:
  push:
    branches:
      - "staging"
jobs:
  build-dev:
    name: 🏗 Update staging from server
    runs-on: self-hosted
    env:
      CONTEXT: .
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
